export interface RatingType {
  region: string;
  category: string;
  number: number;
  percentage: number;
}
export interface CategoryData {
  category: string;
  description: string;
  source: number;
}
